<%= form_for @domain, :url => domains_path(:myuser_id => @myuser.id), :method => :post do |f| %>
  <% if @domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@domain.errors.count, "error") %> prohibited this domain from being saved:</h2>

      <ul>
      <% @domain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Имя %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Примечание %><br />
    <%= f.text_field :descr %>
  </div>
	<div class="field">
		<%= f.check_box :globaly %>
		<%= f.label :Глобальный %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
